# ⚡ НАЧНИТЕ ЗДЕСЬ - Запуск HealthLife AI

## 🎯 Что сейчас запускать

У вас есть 2 варианта:

### ✅ ВАРИАНТ 1: Быстрый старт (только Frontend)

Этот вариант позволит вам **сразу увидеть UI** без настройки базы данных.

```bash
# 1. Установите зависимости
cd apps/web
npm install

# 2. Создайте .env.local файл
copy .env.example .env.local

# 3. Запустите Frontend
npm run dev
```

**Откройте браузер:** http://localhost:3000

Вы увидите:
- ✅ Красивую главную страницу
- ✅ Работающую навигацию между 5 разделами
- ✅ Базовый UI для каждой страницы (Focus, Journey, Coach, You, Tribe)

⚠️ **Что НЕ будет работать:**
- Сохранение данных (нет Backend)
- AI генерация (нет OpenAI)
- Авторизация

---

### 🚀 ВАРИАНТ 2: Полный запуск (Frontend + Backend + Базы данных)

Для полноценной работы приложения.

#### Шаг 1: Установите Docker Desktop

Скачайте и установите: https://www.docker.com/products/docker-desktop/

#### Шаг 2: Создайте .env файлы

```bash
# Корневой .env
copy .env.example .env

# Frontend .env
cd apps/web
copy .env.example .env.local
cd ../..

# Backend .env
cd apps/backend
copy .env.example .env
cd ../..
```

#### Шаг 3: Запустите все сервисы через Docker

```bash
docker-compose up --build
```

Подождите 2-3 минуты. Вы увидите логи запуска всех сервисов.

#### Шаг 4: Проверьте работу

Откройте в браузере:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/api/v1/docs

---

## 📝 ЧТО ВЫ ВИДИТЕ СЕЙЧАС

### Главная страница (/)
Красивый лендинг с кнопкой "Get Started"

### Dashboard (/focus, /journey, /coach, /you, /tribe)
5 основных разделов приложения с базовым UI:

1. **Focus** - Задачи на сегодня с таймлайном
2. **Journey** - Roadmap с фазами прогресса
3. **Coach** - AI чат интерфейс
4. **You** - Статистика и настройки
5. **Tribe** - Сообщество и рейтинг

---

## 🔧 Troubleshooting

### Ошибка: "Module not found"
```bash
cd apps/web
rm -rf node_modules package-lock.json
npm install
```

### Ошибка: "Port 3000 already in use"
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### Docker не запускается
1. Убедитесь, что Docker Desktop запущен
2. Проверьте: `docker --version`
3. Перезапустите Docker Desktop

---

## 📚 Дальнейшие шаги

После того как увидите работающий UI:

1. **Прочитайте документацию:**
   - [QUICKSTART.md](QUICKSTART.md) - детальная инструкция
   - [ARCHITECTURE.md](ARCHITECTURE.md) - концепция приложения
   - [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - структура кода

2. **Начните разработку:**
   - Добавьте реальные API эндпоинты в backend
   - Подключите OpenAI для AI генерации
   - Создайте модели базы данных
   - Реализуйте аутентификацию

3. **Изучите код:**
   - `apps/web/src/app/(dashboard)/` - страницы UI
   - `apps/backend/app/api/v1/endpoints/` - API эндпоинты
   - `apps/backend/app/services/` - бизнес-логика

---

## ❓ Нужна помощь?

Если что-то не работает:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что порты 3000 и 8000 свободны
3. Посмотрите логи в терминале
4. Перезапустите сервер

**Удачи! 🎉**
