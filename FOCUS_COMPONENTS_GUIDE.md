# 🏠 Focus Components - Руководство

## ✅ Что создано

Вы только что создали **4 специфичных компонента для страницы Focus**:

1. **TodayFocus** - Главная цель дня с прогрессом
2. **TaskTimeline** - Таймлайн задач (утро/день/вечер)
3. **EnergyInput** - Интерактивный слайдер энергии
4. **QuickLog** - Быстрое логирование активностей

---

## 🚀 Как посмотреть результат

1. **Откройте страницу Focus:**
   http://localhost:3000/focus

2. **Попробуйте функции:**
   - ✅ Кликайте на задачи чтобы отметить их выполненными
   - 🎯 Нажмите "Mark as Complete" на главной цели
   - ⚡ Двигайте слайдер энергии и сохраняйте
   - 💧 Нажимайте на кнопки Quick Log (Water, Meal, etc.)
   - 🎉 Смотрите уведомления Toast при действиях

---

## 📖 Описание компонентов

### 1. TodayFocus - Главная цель дня

**Файл:** [TodayFocus.tsx](apps/web/src/components/focus/TodayFocus.tsx)

**Что делает:**
- Показывает главную цель дня крупным шрифтом
- Отображает прогресс (0-100%)
- Кнопка для завершения цели
- Анимация при завершении

**Использование:**
```typescript
import TodayFocus from '@/components/focus/TodayFocus'

<TodayFocus
  goal="Walk 10,000 steps"
  description="Your main goal for today"
  progress={60}
  isCompleted={false}
  onComplete={() => console.log('Goal completed!')}
/>
```

**Пропсы:**
- `goal` - текст цели (обязательный)
- `description` - описание цели
- `progress` - прогресс 0-100
- `isCompleted` - завершена ли цель
- `onComplete` - функция при завершении

**Фичи:**
- Градиентный фон (синий → фиолетовый)
- Прогресс-бар с анимацией
- Галочка при завершении
- Адаптивный дизайн

---

### 2. TaskTimeline - Таймлайн задач

**Файл:** [TaskTimeline.tsx](apps/web/src/components/focus/TaskTimeline.tsx)

**Что делает:**
- Группирует задачи по времени дня (утро/день/вечер)
- Показывает прогресс для каждого времени
- Интерактивные чекбоксы
- Цветовое кодирование по времени

**Использование:**
```typescript
import TaskTimeline, { Task } from '@/components/focus/TaskTimeline'

const tasks: Task[] = [
  {
    id: '1',
    title: 'Drink water',
    description: '2 glasses',
    timeOfDay: 'morning',
    isCompleted: false,
    duration: '5 min',
  },
  // ... другие задачи
]

<TaskTimeline
  tasks={tasks}
  onTaskToggle={(taskId) => console.log('Toggled:', taskId)}
/>
```

**Task интерфейс:**
```typescript
interface Task {
  id: string
  title: string
  description?: string
  timeOfDay: 'morning' | 'afternoon' | 'evening'
  isCompleted: boolean
  duration?: string
}
```

**Фичи:**
- Автоматическая группировка по времени
- Иконки для каждого времени (🌅 ☀️ 🌙)
- Счетчик выполненных задач
- Анимация при клике
- Line-through для завершенных

---

### 3. EnergyInput - Слайдер энергии

**Файл:** [EnergyInput.tsx](apps/web/src/components/focus/EnergyInput.tsx)

**Что делает:**
- Интерактивный слайдер 1-10
- Эмодзи для каждого уровня энергии
- Цветовая индикация (красный → зеленый)
- Подсказки для AI адаптации

**Использование:**
```typescript
import EnergyInput from '@/components/focus/EnergyInput'

<EnergyInput
  value={5}
  onChange={(value) => setEnergy(value)}
  onSave={(value) => console.log('Saved:', value)}
/>
```

**Пропсы:**
- `value` - текущее значение 1-10
- `onChange` - функция при изменении
- `onSave` - функция при сохранении

**Уровни энергии:**
| Уровень | Название | Эмодзи | Цвет |
|---------|----------|--------|------|
| 1 | Exhausted | 😴 | Красный |
| 2 | Very Low | 😓 | Красный |
| 3 | Low | 😔 | Оранжевый |
| 4 | Below Average | 😐 | Оранжевый |
| 5 | Average | 😊 | Желтый |
| 6 | Above Average | 😃 | Желтый |
| 7 | Good | 😄 | Лаймовый |
| 8 | Great | 🙂 | Зеленый |
| 9 | Excellent | 😁 | Зеленый |
| 10 | Peak | 🚀 | Темно-зеленый |

**Фичи:**
- Двойное управление: слайдер + кнопки
- Визуальная обратная связь
- Умные подсказки для AI
- Кнопка сохранения

---

### 4. QuickLog - Быстрое логирование

**Файл:** [QuickLog.tsx](apps/web/src/components/focus/QuickLog.tsx)

**Что делает:**
- Кнопки для частых действий
- История последних активностей
- Toast уведомления при логировании

**Использование:**
```typescript
import QuickLog from '@/components/focus/QuickLog'

<QuickLog
  onLog={(actionId) => console.log('Logged:', actionId)}
/>
```

**Доступные действия:**
- 💧 Water (вода)
- 🍽️ Meal (еда)
- 💪 Workout (тренировка)
- 🚶 Walk (прогулка)
- 😴 Sleep (сон)
- 🧘 Meditate (медитация)

**Кастомные действия:**
```typescript
const customActions = [
  { id: 'coffee', label: 'Coffee', emoji: '☕', color: 'bg-brown-100', unit: 'cup' },
]

<QuickLog actions={customActions} onLog={handleLog} />
```

**Фичи:**
- Цветовое кодирование
- Hover эффекты
- История активностей
- Единицы измерения

---

## 🎨 Обновленная страница Focus

**Файл:** [focus/page.tsx](apps/web/src/app/(dashboard)/focus/page.tsx)

**Структура:**

```
┌─────────────────────────────────────────┐
│  Header (дата и название)               │
├─────────────────────────────────────────┤
│  TodayFocus (главная цель)              │
├─────────────────────────────────────────┤
│  ┌──────────────┬────────────────────┐  │
│  │ TaskTimeline │  EnergyInput       │  │
│  │  (2/3)       │  (1/3)             │  │
│  │              ├────────────────────┤  │
│  │              │  QuickLog          │  │
│  │              │                    │  │
│  └──────────────┴────────────────────┘  │
├─────────────────────────────────────────┤
│  Stats Footer (статистика)              │
└─────────────────────────────────────────┘
```

**Функциональность:**
- ✅ Управление состоянием задач
- ✅ Подсчет прогресса
- ✅ Toast уведомления при действиях
- ✅ Адаптивная сетка (desktop/mobile)
- ✅ Реал-тайм обновления

---

## 💡 Примеры использования

### Отметить задачу как выполненную

```typescript
const handleTaskToggle = (taskId: string) => {
  setTasks((prev) =>
    prev.map((task) =>
      task.id === taskId
        ? { ...task, isCompleted: !task.isCompleted }
        : task
    )
  )

  // Показать уведомление
  showToast({
    type: 'success',
    title: 'Task completed!',
  })
}
```

### Сохранить уровень энергии

```typescript
const handleEnergySave = (value: number) => {
  setEnergyLevel(value)

  // Отправить на Backend
  await fetch('/api/energy', {
    method: 'POST',
    body: JSON.stringify({ energy: value }),
  })

  showToast({
    type: 'info',
    title: 'Energy level saved',
  })
}
```

### Добавить Quick Log запись

```typescript
const handleQuickLog = async (actionId: string) => {
  // Отправить на Backend
  await fetch('/api/logs', {
    method: 'POST',
    body: JSON.stringify({
      action: actionId,
      timestamp: new Date(),
    }),
  })

  // Toast уже показывается автоматически в QuickLog
}
```

---

## 🔌 Интеграция с Backend (позже)

Когда Backend будет готов, замените mock данные на реальные API вызовы:

### Загрузка задач

```typescript
// Вместо mockTasks
const { data: tasks } = useQuery({
  queryKey: ['tasks', 'today'],
  queryFn: async () => {
    const res = await fetch('/api/tasks/today')
    return res.json()
  },
})
```

### Обновление задачи

```typescript
const { mutate: updateTask } = useMutation({
  mutationFn: async (taskId: string) => {
    await fetch(`/api/tasks/${taskId}`, {
      method: 'PATCH',
      body: JSON.stringify({ isCompleted: true }),
    })
  },
  onSuccess: () => {
    queryClient.invalidateQueries(['tasks', 'today'])
  },
})
```

---

## 📊 Статистика (Footer)

Сейчас показывает:
- ✅ Tasks Completed (выполнено задач)
- 📈 Completion Rate (процент выполнения)
- ⚡ Energy Level (уровень энергии)
- 🔥 Current Streak (дни подряд)

Можно добавить:
- 🏆 Points earned today
- 📅 Days this week
- 🎯 Weekly goal progress

---

## 🎯 Следующие шаги

### Вариант 1: Добавить анимации

```typescript
import { motion } from 'framer-motion'

<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.3 }}
>
  <TodayFocus {...props} />
</motion.div>
```

### Вариант 2: Добавить персистентность

Сохранение в localStorage:

```typescript
useEffect(() => {
  localStorage.setItem('tasks', JSON.stringify(tasks))
}, [tasks])
```

### Вариант 3: Создать компоненты Journey

Следующие компоненты из TODO:
- RoadmapVisualizer
- MilestoneCard
- WeeklyReview

---

## 📝 Краткая справка

```typescript
// Импорт
import {
  TodayFocus,
  TaskTimeline,
  EnergyInput,
  QuickLog,
} from '@/components/focus'

// TodayFocus
<TodayFocus goal="..." progress={50} onComplete={...} />

// TaskTimeline
<TaskTimeline tasks={tasks} onTaskToggle={handleToggle} />

// EnergyInput
<EnergyInput value={5} onSave={handleSave} />

// QuickLog
<QuickLog onLog={handleLog} />
```

---

**Отличная работа! Страница Focus полностью функциональна.** 🎉

Откройте [http://localhost:3000/focus](http://localhost:3000/focus) и протестируйте все компоненты!
